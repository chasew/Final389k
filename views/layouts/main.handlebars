<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" type="text/css" href="/public/css/main.css">
  <title>Final Project</title>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.js"></script>
</head>
<div class="nav">
    <h1>College Park Restaurant Ratings</h1>

    <div class="dropdown">
        <button class="dropbtn">Tags</button>

        <div class="dropdown-content">
                {{#each tags}}
                <a href="/tag/{{this}}" >{{this}}</a>
                {{/each}}

        </div>

    </div>
    <a href="/" class="tag">Home</a>
    <a href="/tag/5Star" class="tag">5 Stars</a>
    <a href="/nav/Newest" class="tag">Newest</a>
    <a href="/nav/Alphabetical" class="tag">Alphabetical</a>
    <a href="/nav/Random" class="tag">Random</a>
    <a href="/chatRoom" class="tag">Live Discussion</a>
    <form id="inputForm">

        {{#if input}}
            <input id="input" type="text" value={{{input}}} onkeyup="myInputFunction(this.value)" />
        {{else}}
            <input id="input" type="text" placeholder="Search" onkeyup="myInputFunction(this.value)" />
        {{/if}}


    </form>


</div>

<body bgcolor="#223843">
    <br>
    <div class="buttonGroup">
        <button type="button" id="createNew">Enter a review</button>
        <button type="button" id="aboutNew">Click to Learn About Us!</button>
        <button type="button" id="extraNew">Try Me!</button>
        <br />
        <br />
        {{{body}}}
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.js"></script>
        <br />
        <br />
    </div>
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.0/jquery.min.js"></script>
    <script>var socket = io();</script>
    <script>
        $('#createNew').on('click', function () {
            //console.log("hi");
            window.open("/create");
        });
    </script>
    <script>
        $('#aboutNew').on('click', function () {
            //console.log("hi");
            window.open("/about");
        });
    </script>
    <script>
        $('#extraNew').on('click', function () {
            //console.log("hi");
            window.open("/extra");
        });
    </script>

    <script>
        function myInputFunction(val) {
            var str = "/nav/Search/" + val;
            $('input').val(val);
            //var myWindow = window.open(str, "", "", true);
            var myWindow = window;
            $('input').val(val);
            var textNode = myWindow.document.createTextNode(val);
            $('input').val(val);
            myWindow.document.getElementById("input").appendChild(textNode);
            myWindow.location.replace(str);
            $('input').val(val);
            myWindow.document.getElementById("input").appendChild(textNode);
            $('input').val(val);
        }
    </script>
</body>

</html>